pywebview==5.4
requests>=2.31
# Leitura do PDF do guia para ordenar o walkthrough automaticamente.
pypdf>=4.0
# Decodificação das imagens incorporadas em PDFs para o Guia Inteligente.
Pillow>=10.0
# Importar guias direto do GameFAQs: curl_cffi imita o TLS de um Chrome atual;
# cloudscraper fica como fallback e BeautifulSoup lê o HTML.
curl_cffi>=0.16.0
cloudscraper>=1.2.71
beautifulsoup4>=4.12
# Refino opcional do guia por IA. Só o provedor Anthropic precisa de SDK;
# Gemini e endpoints compatíveis com a OpenAI usam REST via `requests`.
anthropic>=0.69
# No Windows o backend EdgeChromium usa pythonnet. Fixamos um combo estavel e
# testado de ponta a ponta; com pywebview 6.x + pythonnet 3.1.x a introspecao
# da js_api chega a estourar a recursao ao percorrer window.native.
# (A causa raiz do bridge vazio foi corrigida no engine.py tornando o atributo
#  window privado; este pin e uma garantia extra.)
pythonnet==3.0.5 ; sys_platform == "win32"
